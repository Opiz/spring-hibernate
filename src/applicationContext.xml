<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context.xsd">


    <!-- component-scan detects the annotations by package scanning -->
    <context:component-scan base-package="no.skatteetaten.opiz"/>

    <!-- property-placeholder is used to externalize properties in a separate file -->
    <context:property-placeholder location="resource/application.properties"/>

    <!-- property-placeholder is used to externalize properties in a separate file -->
    <context:property-placeholder location="resource/fortunes.txt"/>



</beans>